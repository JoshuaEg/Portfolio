This assignment required us to make a parser and evaluated for a dummy programming language in Ocaml. My work is destributed in
lexer.ml (tokenizes input), parser.ml (parses the tokens into an abstract syntax tree), and eval.ml (evaluates code).

Not the hardest assignment in this course, but I think its important to show I have some insight on things like this.